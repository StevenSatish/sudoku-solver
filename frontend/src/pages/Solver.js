import SideNav from "../components/SideNav";

function Solver() {
  return (
    <SideNav>
      <div>
        <h1>Pathfinder Solver</h1>
        <p>Here is the solver page</p>
      </div>
    </SideNav>
  );
}

export default Solver;
